h3 Demo of Directives

div(kj-clock, frequency='2000')
div.pull-right(kj-locate)
div.pull-right(kj-weather, zip='37216')
hr
div
.row
  input(type='text', ng-model='title')
  button(ng-click='addMovie()') Add Movie
.row
  div(kj-movie, ng-repeat='title in titles', title='{{title}}', remove='delMovie($index)')
  div(ng-repeat='person in people', kj-greeting, name='{{person.name}}', age='{{person.age}}')
  div
  div(kj-stock, ng-repeat='symbol in symbols', symbol='{{symbol}}')
